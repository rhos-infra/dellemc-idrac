- name: Performing Power Action '{{ power_action | default('') }}'
  dellemc_change_power_state:
    idrac_ip: "{{ idrac_ip }}"
    idrac_port: "{{ idrac_port }}"
    idrac_user: "{{ idrac_user }}"
    idrac_pwd: "{{ idrac_pass }}"
    change_power: "{{ power_action }}"
